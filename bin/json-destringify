#!/usr/bin/env python3

import json
import sys

# parse escaped json *twice* to unescape quotes, etc.
escaped = json.load(sys.stdin)
data = json.loads(escaped)

# convert back to json for printing
print(json.dumps(data, indent=None))

